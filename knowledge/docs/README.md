# Generated knowledge documents

Markdown in this folder is generated from approved JSON datasets. Use the corresponding template in `templates/` and retain the source dataset name and generation date in the rendered document.

Do not generate documentation directly from `raw/` or `processed/` content.
