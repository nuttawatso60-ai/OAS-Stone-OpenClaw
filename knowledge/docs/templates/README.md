# Markdown templates

Each template is rendered from the dataset with the same name in `../../datasets/`. Replace the `{{...}}` placeholders only from approved, de-identified dataset values.

Rendered documents should include the dataset version and generation date.
